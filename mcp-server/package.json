{
  "name": "@hemachain/mcp-server",
  "version": "0.0.0",
  "private": true,
  "description": "MCP server wrapping Foundry CLI and HemaChain traceability queries — placeholder, implementation in Phase 7",
  "scripts": {
    "start": "echo 'mcp-server/ — not yet implemented. See ../docs/TRACK.md Phase 7 and ../docs/MCP.md.' && exit 0"
  },
  "engines": {
    "node": ">=20"
  }
}
