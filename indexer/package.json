{
  "name": "@hemachain/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Event indexer for HemaChain contracts — ethers WS subscription + SQLite + SSE stream + alert daemon (SDD §10.2)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "ethers": "^6.13.4"
  }
}
